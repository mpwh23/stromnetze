# In die bestehende server { ... } Site fuer home.moja.de einfuegen.
# Wichtig: Dieses Snippet ersetzt nicht die komplette Site.

location = /strom {
    return 301 /strom/;
}

location /strom/api/ {
    proxy_pass http://127.0.0.1:8088/api/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

location /strom/ {
    alias /var/www/strom/;
    try_files $uri $uri/ /strom/index.html;
}
